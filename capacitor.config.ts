import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.notarypro.vecinos',
  appName: 'Vecinos NotaryPro POS',
  webDir: './client/dist'
};

export default config;
